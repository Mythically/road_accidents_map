library(shiny)

shinyUI(fluidPage(
  titlePanel("UK Road Accidents"),
  sidebarLayout(
    sidebarPanel(
      # add input widgets here
    ),
    mainPanel(
      # add output plots here
    )
  )
))